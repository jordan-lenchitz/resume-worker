{
  "name": "resume-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy -c .config/wrangler.jsonc",
    "dev": "wrangler dev -c .config/wrangler.jsonc",
    "start": "wrangler dev -c .config/wrangler.jsonc",
    "test": "vitest -c .config/vitest.config.mts",
    "cf-typegen": "wrangler types -c .config/wrangler.jsonc"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.8.0",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.92.0"
  }
}